# Customer dashboard header/profile patch

تغییرات:
- هدر لوگو/اسم لکسارا مثل لندینگ به بالای `/dashboard/customer` اضافه شد.
- هدر با اسکرول به بالا می‌رود و محو می‌شود.
- badge طلایی «حساب کاربر» از کارت پروفایل حذف شد.
- نام و نوع حساب کاربری همراه با پروفایل مرکزتر شد.
- آپلود عکس پروفایل اضافه شد؛ اگر عکس نباشد حرف اول نام نمایش داده می‌شود.
- بک‌اند و serializer برای دریافت `avatar` در `PATCH /api/auth/me/update/` آماده شد.
- footer خروج کوچک‌تر و جمع‌وجورتر شد.
