# Register Mobile Logo + Button Patch

تغییرات:
- در صفحه `/register`، Navbar کامل قبلی حذف شد و فقط لوگوی ساده لکسارا در مرکز بالای صفحه باقی ماند.
- در نسخه موبایل، لوگو مانند لندینگ با اسکرول به بالا حرکت می‌کند و محو می‌شود.
- دکمه‌های «ثبت‌نام کاربر» و «ثبت‌نام وکیل» در نسخه موبایل کوچک‌تر شدند تا با خط دوم متن کیوسک تداخل نداشته باشند.
- فقط فرانت تغییر کرده است.
