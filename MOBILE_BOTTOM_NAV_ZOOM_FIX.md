# Mobile Bottom Navigation Zoom Fix

- Removed scale animation from bottom nav buttons on tap.
- Added fixed height, overflow protection, ellipsis and `touch-action: manipulation`.
- Normalized button appearance for account and more buttons on mobile.
- Added small-screen rules so the 4 bottom buttons never overflow the mobile viewport.
